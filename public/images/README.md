# Hero poster

Drop the hero poster fallback image here as:

    hero-poster.jpg

It is referenced by the homepage hero (`src/components/home/Hero.tsx`) at the
public path `/images/hero-poster.jpg`. It shows before the video loads and as a
fallback when video can't autoplay. Use a frame that matches the video, ~1920x1080.
